{
  "name": "voidcord",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install:express": "npm i express",
    "install:axios": "npm cache clean --force && npm install && npm i axios && npm i express",
    "build": "echo \"Running server\" && npm run install:axios && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.5.0",
    "express": "^4.18.2",
    "fs": "^0.0.1-security",
    "http": "^0.0.1-security"
  }
}
